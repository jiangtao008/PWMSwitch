# PID 速度闭环调参指南

> 适用平台：PWMSwitch (STM32F103 + MG513 履带底盘)
> 相关参数定义在 `src/control.c` 顶部宏

---

## 目录

1. [参数一览](#1-参数一览)
2. [标定 MAX_SPEED](#2-标定-max_speed)
3. [调 KP（比例）](#3-调-kp比例)
4. [调 KI（积分）](#4-调-ki积分)
5. [调 KD（微分）](#5-调-kd微分)
6. [完整调参流程](#6-完整调参流程)
7. [常见问题](#7-常见问题)
8. [附录：PID 原理简述](#8-附录pid-原理简述)

---

## 1. 参数一览

所有参数都在 `src/control.c` 顶部：

```c
#define PID_MAX_SPEED_CPS   9500.0f   // 满油门时的编码器 counts/sec
#define PID_KP              80.0f     // 比例增益 (归一化误差 ±1.0)
#define PID_KI              30.0f     // 积分增益
#define PID_KD              0.0f      // 微分增益（暂不启用）
#define PID_INTEGRAL_LIMIT  2.0f      // 积分上限 ±2.0
```

**归一化误差设计**：误差经过 `(setpoint - measurement) / MAX_SPEED` 映射到 ±1.0，所以 Kp/Ki 的含义与速度量程无关。换轮子、换电机后只需要改 `MAX_SPEED`，Kp/Ki 基本不用动。

### 参数物理意义

| 参数 | 含义 | 效果 |
|------|------|------|
| `Kp = 80` | 100% 速度误差 → 80% PWM | 决定响应快慢 |
| `Ki = 30` | 1 秒全误差累积 → 30% PWM | 消除静摩擦/稳态误差 |
| `integral_limit = 2.0` | 积分±2 → 稳态 I 项上限 ±60% PWM | 防 windup |

---

## 2. 标定 MAX_SPEED

**这是第一步，也是最重要的一步。** 如果 `MAX_SPEED` 不准，setpoint 和物理速度对不上，后面 Kp/Ki 怎么调都没用。

### 标定方法

```
1. 设 Kp = 0, Ki = 0 （开环直驱）
2. 充满电，车架起来（轮子离地）
3. 遥控器推到满油门
4. 观察 OLED 上编码器位置数值，看 1 秒跳了多少
   或串口打印 SpeedSensor_GetPosition() 差值
5. 把这个数填到 PID_MAX_SPEED_CPS
```

### MG513 底盘的基准值

以你的配置（13 PPR × 4 正交 × 30:1 减速）：
- 满速 counts/sec 约 **9500**
- **不同电池电压会影响实际值** — 满电和快没电时不一样，取平均值

---

## 3. 调 KP（比例）

### 效果

Kp 决定"速度偏差 → 多大 PWM 修正"。Kp 越大响应越快，但太大会持续振荡（电机忽快忽慢）。

### 调法

从 20 开始，每次 ×1.5–2.0 递增：

| Kp | 现象 | 判断 |
|----|------|------|
| 0 | 电机不转 | 开环无反馈 |
| 10–20 | 响应很肉，推杆后慢半拍 | 偏低 |
| 40–60 | 响应跟上手感 | 还行 |
| **80** | 响应跟手，轻微过冲可接受 | **目标区** |
| 120+ | 持续振荡（电机声音忽高忽低） | 偏高，退回 60% |

### 判断标准

把车拿在手里（轮子离地），快速推放油门：
- **偏小**：推杆后电机慢悠悠加速，收杆后慢悠悠减速
- **正好**：推杆电机立刻响应，收杆立刻停，不冲不拖
- **偏大**：电机发出振荡声，速度不稳

找到振荡点后 **×0.6** 作为工作值。

---

## 4. 调 KI（积分）

### 效果

Ki 负责消除"静态误差"——比如车停在有摩擦的地面，P 项推不动，I 项慢慢累积到够大的 PWM 让车动起来。

### 调法

Kp 调好后开始加 Ki：

| Ki | 现象 | 判断 |
|----|------|------|
| 0 | 低速起步顿挫，推一点不动、再推就冲 | 偏低 |
| 10–20 | 起步顺畅一些 | 还行 |
| **30** | 从静止到低速匀速过渡平滑 | **当前值** |
| 60+ | 急收油门后回中过量（overshoot） | 偏高，退回 60% |

### 判断标准

放地上低速推油门（5–10% 油门）：
- **偏小**：车不动，再推多一点突然窜出去（俗称"开关量手感"）
- **正好**：缓慢均匀起步，速度与摇杆位置线性
- **偏大**：急收油门后车还会往前多冲一点

### 关于 INTEGRAL_LIMIT

`integral_limit = 2.0` 限制积分累积的幅度上限。如果发现**爬长坡后平路松油门，车还在自己往前走一会儿**，说明积分累积太多释放慢——把 `integral_limit` 适当调小（如 1.0–1.5）。

---

## 5. 调 KD（微分）

### 效果

Kd 起到"阻尼"作用——当速度快速变化时反推 PWM 来抑制振荡。**对于履带车（大惯性），通常不需要 Kd**，除非 Kp 调太高后持续振荡且 Ki 调小也压不住。

### 调法

```
1. 先确认 Kp 已经退回振荡点的 60%
2. Kd 从 5 开始试，最大不超过 30
3. 有效果就留，没效果就保持 0
```

### 副作用

Kd 对噪声敏感——编码器抖动会被 Kd 放大为 PWM 跳变，导致电机异响。如果上了 Kd 电机声音变毛糙，说明微分项放大了测量噪声，这种情况宁可不加 Kd 也要降低 Kp。

---

## 6. 完整调参流程

### 快速版

```
① MAX_SPEED 标定  →  ② Kp 调到跟手不振荡  →  ③ Ki 调到起步线性
```

每一步确认没问题再进下一步。

### 详细版

```
┌──────────────────────────────────────────┐
│ ① 标定 MAX_SPEED                        │
│    Kp=0, Ki=0 → 满油门测 counts/sec     │
├──────────────────────────────────────────┤
│ ② 粗调 Kp                               │
│    从 20 起步，×2 递增，找到振荡点       │
│    设为振荡点 × 0.6                     │
├──────────────────────────────────────────┤
│ ③ 加 Ki                                 │
│    从 10 起步，每次 +10                  │
│    调到起步顺畅、低速线性              │
├──────────────────────────────────────────┤
│ ④ 微调 Kp                               │
│    Ki 加上后可能会增加过冲              │
│    适当降一点 Kp（降 10–20%）           │
├──────────────────────────────────────────┤
│ ⑤ 场景验证                              │
│    放地上跑：直行、转弯、起步、停车    │
│    爬坡、下坡、越障                    │
│    不同电量下测试                      │
├──────────────────────────────────────────┤
│ ⑥ 可选：加 Kd 压谐振                    │
│    只在有高频振荡时才加                │
└──────────────────────────────────────────┘
```

### 场景验证 checklist

| 场景 | 要检查的现象 |
|------|------------|
| 怠速（摇杆中位） | 电机不自己转，不嗡嗡响 |
| 低速直行（10–20%） | 起步均匀不卡顿，左右速度一致 |
| 中速直行（50%） | 速度平稳不忽快忽慢 |
| 急加速 | 响应快、不滞后 |
| 急减速/急停 | 不 overshoot、不继续滑行 |
| 原地转向 | 左右轮速度相反、对称 |
| 行进中转向 | 差速转向流畅不卡顿 |
| 爬坡 | 不掉速（或掉速后能恢复） |
| 不同电量 | 满电和快没电时手感差异尽量小 |

---

## 7. 常见问题

### Q: 电机一通电就自己慢慢转（不推摇杆）

原因：PID 积分在累积一个小的偏差，或者 `MAX_SPEED` 偏小导致任何微小摇杆偏差都被放大。

解决：
- 检查摇杆中位校准（`in_ch_1`、`in_ch_2` 是否真的 = 50）
- 检查 `MAX_SPEED` 是否偏小
- 降低 `Ki` 或暂时设 `Ki=0` 验证

### Q: 电机声音异常，嗡嗡响或抖

原因：PWM 频率偏低或 PID 振荡

解决：
- 确认 `pwm_output_freq_hz = 1800` (1.8 kHz)
- 降低 `Kp` 或用 Kd 阻尼

### Q: 左右车轮速度不一致，车子走不直

原因：两个电机/编码器有差异，或者 PID 参数不一致

解决：
- 检查左右 `MAX_SPEED` 是否一致（代码里共用一个宏）
- 检查左右编码器接线——TIM1（左）和 EXTI（右）的正交极性是否一致
  - 如果一边正转计数增加、另一边减少，说明 A/B 相接反了或 ISR 方向表反了
  - 可以在 `control.c` 里对某一侧取反：`speed = -speed`

### Q: 急收油门后车轮继续转一会儿（积分 windup）

原因：积分项累积了太多值，释放慢

解决：
- 减小 `PID_INTEGRAL_LIMIT`
- 或者减小 `Ki`

### Q: 车子在平地和坡上速度差异很大

原因：Ki 不够大，无法补偿坡道阻力

解决：
- 增加 `Ki`（但要接受平地急停时多一点 overshoot）
- 这是速度和力控的折衷，无法完全消除

### Q: 随着电池电压降低，手感越来越肉

开环直驱时电池电压直接影响电机速度，闭环 PID 会自动补偿——但前提是 MAX_SPEED 是按满电标定的。

如果低电量时感觉明显变肉：
- 可以在 `main.c` 里检测电池电压（如果有 ADC），动态调整 MAX_SPEED
- 或者接受低电量时性能下降（简单方案）

---

## 8. 附录：PID 原理简述

### 公式

```
输出 = Kp × 误差 + Ki × ∫误差 dt + Kd × d(误差)/dt
```

### 三项的角色

```
  ┌─ 比例项 (P) ──────────────────────────┐
  │  误差 × Kp                            │
  │  立刻响应，但不能消除稳态误差         │
  │  太大会振荡                          │
  └───────────────────────────────────────┘
  ┌─ 积分项 (I) ──────────────────────────┐
  │  误差累积 × Ki                        │
  │  慢慢累积消除静摩擦/外部阻力         │
  │  累积太多会导致 overshoot/windup     │
  └───────────────────────────────────────┘
  ┌─ 微分项 (D) ──────────────────────────┐
  │  误差变化率 × Kd                      │
  │  起阻尼作用，抑制振荡                │
  │  放大会放大测量噪声                  │
  └───────────────────────────────────────┘
```

### 本项目的实现

```c
// 归一化误差（无关速度量程）
float error = (setpoint - measurement) / PID_MAX_SPEED_CPS;

float p = pid->kp * error;                         // 比例

pid->integral += error * dt;                       // 积分
// ↑ 加了 anti-windup 限幅
float i = pid->ki * pid->integral;

float d = pid->kd * (error - pid->prev_error) / dt; // 微分
pid->prev_error = error;

float out = p + i + d;  // 输出 -100 ~ +100 → 正/反转 PWM
```

反 windup（积分限幅）的实现：

```c
if (pid->integral >  pid->integral_limit)
    pid->integral =  pid->integral_limit;
if (pid->integral < -pid->integral_limit)
    pid->integral = -pid->integral_limit;
```

**目的**：当误差长时间存在（如爬长坡），积分不会无限制累积到上千。松油门后积分需要衰减回正常范围才有意义——限幅后衰减时间可控。
